# Changelog

Releases are cut as annotated git tags. `git tag -l -n20 <tag>` shows the full
message for any release; `git log <old>..<new>` shows the commits between two.

This file records highlights. It is **not** exhaustive per-tag — the tags
themselves are the authoritative record.

## Recent releases

| Version | Date | Highlights |
|---|---|---|
| v0.9.76 | 2026-08-27 | **The daemon records its code, says why it stops, and gets noticed when it dies — and `detect-only` finally reports `ok`.** `scheduler.sh` now writes `~/.ilk-data/scheduler.state.json` (`{pid, started_at, toolkit_head}`) on startup, which is the piece every `stale-daemon` report was missing: with a recorded head to compare against, `bounce_daemons.sh` can *prove* freshness instead of taking the settled absent-⇒-stale path. Measured immediately after: `host_deploy_status.py` returned **`ok`, exit 0** for the first time since Phase 4 detection was built — before this, every host on every release reported `stale-daemon` by construction, and no upgrade could ever avoid a bounce. `loop_status` now passes `runtime_dir`, so it and `ship_audit` stop disagreeing about whether a sub-plan is proven — the `SHIP PROOF MISSING` banner that fired on sub-plans holding valid `#ship` markers is gone. **The scheduler now says why it stopped.** Its lock-release EXIT trap at `:53` was silently **clobbered** by a second `trap … EXIT` at `:66` — a second registration replaces the first in bash, verified by experiment — so `release_scheduler_lock` never ran and `scheduler.pid` survived every exit. That left each start depending on `ilk_pid_alive` judging a leftover PID dead, and a *recycled* PID would have made the scheduler log `already running`, exit 0, and never be relaunched (`KeepAlive {SuccessfulExit: false}`) — the outcome `scheduler.sh:26` already called unrecoverable. Both handlers now run from one cleanup function, and SIGTERM/SIGINT/SIGHUP are trapped, logged with the signal and status, and release the lock. This matters because the scheduler died **four times** on 2026-08-26/27 and each death was undiagnosable: the log simply stopped mid-poll-cycle. **A dead scheduler now gets noticed** — a separate health-check agent reports `loaded`/`absent`/`held` and re-bootstraps an absent one unless `$ILK_DATA/scheduler.hold` says an operator stopped it deliberately. `KeepAlive {SuccessfulExit: false}` was deliberately left alone: dropping it would rapid-fire on the `already running` exit-0 until launchd throttled the agent out of existence. The cost of its absence was concrete — MASTER-2026-08-26e sat `queued` for ~26 minutes with nothing polling to promote it. **Phase 0 refused this release once**, on 6 undeclared failures all in the new exit-reason test file: its harness set `HOME` and claimed "HOME overrides all scheduler paths", but `scheduler.sh` resolves `ILK_DATA_HOME` → `ILK_DATA_DIR` → `$HOME/.ilk-data`, so an inherited value beat `HOME` and the tests read the real `~/.ilk-data` — including the live scheduler's pidfile. The same gap broke the v0.9.74 gate and was fixed there in `33a2712`. Pinned; verified with the real scheduler live, the exact condition that produced the failures: **6 failed → 14 passed**. After: verdict `pass`, **31 excused with nothing added to `baseline_red`, 0 undeclared**, collection floor **2395**, baseline-diff vs v0.9.75 **0 regressions, 31 inherited**. |
| v0.9.75 | 2026-08-27 | **The bounce that reports honestly now also recovers, and an already-current host stops lying about its daemon.** Two fixes, both found by running `/ilk-ship` Phase 4 against live hosts rather than by reading code. **A bounce now retries before declaring failure.** v0.9.74 correctly reported `unreachable: scheduler (bounce failed to restore: bootstrap exit 5)` and exited 2 — but it never retried, so every bounce risked leaving the daemon down. `launchctl bootstrap` returns **exit 5** when issued immediately after `bootout` (the domain has not settled) and a plain retry succeeds; that is the **confirmed root cause of two live scheduler outages** on 2026-08-26, both of which had been suspected to be tests reaching the real daemon and were neither. The bounce is now bounded at 3 attempts with a settle wait, reports `unreachable` only once they are exhausted, and a test asserts the bound is exactly 3 — *not 2, not 4* — because an unbounded retry would hang the upgrade. **`already current` no longer skips the bounce.** `do_apply` returned at `:331` after `echo "already current"` and `reconcile_links ""`, while `bounce_stale_daemons` sat unreachable at `:369`. Verified on a host at HEAD: a clean `--apply` printed no `bouncing:`/`stale:` line at all and detect-only still reported `stale-daemon` afterwards — so the host with nothing to pull, which is precisely the host whose daemon has been up longest, was the one never bounced. A daemon's staleness is a property of the running process, not of whether the tree moved. `--check` remains side-effect free. **Two claimed findings were retracted before any code was written.** One blamed current `upgrade.sh` for a refusal whose message only exists in **v0.9.73** — the host was 36 commits behind, and current code cannot emit that label at all (grep: 0 hits). The other read `[ok] symlink` as proof of a created link, when `install.sh` prints it on **every** idempotent run for links that already resolve (verified by running it twice). Both errors were the same shape: an observed output taken as evidence of a mechanism without establishing what the output meant. Release evidence: Phase 0 **2 of 2 proven** on the first pass, gate verdict `pass` at a fresh `head_sha`, collection floor **2347** collected, baseline-diff vs v0.9.73 **0 regressions, 31 inherited**, nothing added to `ship.baseline_red`. This release also stores a suite baseline — v0.9.74's manual Phase 3 omitted one, which is why 26e's diff had to reach back two tags. |
| v0.9.74 | 2026-08-26 | **A bounce that does not restore is not a bounce — and the fix for a false `ok` shipped its own false `ok`.** Two batches release together: 26b gave the toolkit one `bounce_daemons.sh` with two callers (`ilk-upgrade` bounces; Phase 4 detects), staleness decided from a recorded `toolkit_head` versus tree HEAD, and `bootout`+`bootstrap` that never `kill`s. Then **the live scheduler was left booted-out on this host during that very batch** — `launchctl print` said "Could not find service" while `print-disabled` still said `enabled`, the plist was present and `scheduler.pid` held a dead 39734; the poll log stopped at 17:49:55. Enabled-but-absent is what a bare `bootout` with no successful `bootstrap` leaves behind. The chain was fully reachable in shipped code: `bounce_daemons.sh:168` ran `bootstrap` **unchecked** under `set -euo pipefail` (`:23`), so a failure aborted the script before `bounced=1` (`:169`) and before the 2/1/0 exit block (`:176-180`), exiting with launchctl's own code; `upgrade.sh:290-294` has no `case` arm outside 0/1/2 and `:370` swallowed it with `|| true`; `host_deploy_status.py:89` treats `exit_code == 2` as the *only* failure signal, so `:97` returned **`ok`** for a host whose daemon was down. 26d closes it: bootstrap's status is captured and the daemon **verified back** before anything is called bounced, a failed restore is `unreachable` and never `bounced`, the resolver **fails closed** on output it cannot parse, and a contract test pins the producer's real stdout to the consumer's parse — neither side had pinned the format (producer tests asserted loose substrings; **0 of 20** matched a line prefix). Two more 26b defects: `host_deploy_status.py` had no `__main__`, no `argparse` and **0 callers repo-wide**, so Phase 4 still hand-parsed from prose — it now has a CLI that `SKILL.md` invokes per declared host, guarded by a test asserting the doc names the script. And AC-6, the anti-stale-host guarantee, was **verified by a tautology**: the test built `results` by iterating `hosts` then asserted `set(results.keys()) == set(hosts)`, which cannot fail; a production `resolve_hosts()` and a drop test replace it. **Phase 0 refused this release twice.** The batch gate recorded `fail` with **5 undeclared failures** and 7 declared steps carried no commit. Four of the five were one root cause, reproduced deterministically: the bounce harnesses overrode `HOME` but inherited `ILK_DATA_HOME` through `**os.environ`, and three tests leak it via raw `os.environ` writes — so the state file read as absent and the settled "absent ⇒ stale ⇒ bounce once" rule fired on a **fresh** daemon (`ILK_DATA_HOME=/tmp/does-not-exist pytest` → 3 failures; after pinning → 35 passed). The fifth: the new PATH deny-shim for shell-spawned `launchctl` always exited 126, making `ILK_TEST_GUARD_REPORT` a lie for anything reached through a shell while the Popen layer honoured it at `conftest.py:274-277`. The 7 missing steps had landed their content but never run their gates, so they were attested by **running each declared gate at release time** rather than by adding a marker. After: verdict `pass`, **31 excused by `ship.baseline_red` with nothing added to it, 0 undeclared**, and baseline-diff **0 regressions across 2339 collected tests vs v0.9.73**. |
| v0.9.73 | 2026-08-26 | **The gate that measures a batch, and the guards that watch the suite, all reported success while doing nothing.** Every finding here came from `/ilk-ship` Phase 0 refusing to release, and each was proven by probe rather than inference. **The batch gate never persisted a verdict** — `write_record` had one non-test caller, inside `except Exception`, so a run that printed `verdict=fail` left a 3-hour-old `pass` on disk. **Its poll could never succeed**: `wait_for_background_output.sh` greps for an `[exited with code N]` marker the *harness* writes when it auto-backgrounds a Bash call, and `subprocess.Popen` writes no such marker — **0 occurrences in 3063 lines** of a real output file, so every gate burned its full 600s bound and returned 125, making `fail` unconditional. Measured after: 327.42s suite, 327.98s total, **0.56s of overhead where 263s used to be**. **`main()` exited 0 on every verdict**, so the runner's correctly-fixed `\|\| gate_exit=$?` idiom printed a 32-failure suite under "Gate completed." **The running marker was never released** and refused on file presence, so one completed run permanently disabled the gate for that project. **The record had two homes** — the runner passed `external_launcher_dir`, the module's own default was `external_runtime_dir` — and `ship_audit`'s CLI had no `--runtime-dir` at all, so every sub-plan read "no gate result recorded" whatever the gate wrote. **`ship.suite.timeout` was ignored**: gh-resolve declares 1800 and its suite measures 925.79s, but the gate used a hard-coded 600. **`shipped` was self-reported.** Nothing in the runner writes it; `evaluate_ship` asked only "was the gate green?", so `evaluate_ship('shipped', <4 declared gates>, {'all_passed': True})` returned *honest* for a sub-plan that had committed 2 of 4 steps. Step-commit counting existed only in `ship_audit`, which runs after the batch is over. **The host-mutation guard sat in `skills/ilk-loop/tests/conftest.py`** — pytest scopes a conftest to its own tree, and the offender it was written for lives in `skills/ilk-watchdog/tests/`, which reported `.....s` with its `launchctl` calls passing straight through. **Its session ledger was dead too**: the exemption fixture was `autouse` and unconditional, so every test dropped its recordings and `pytest_sessionfinish` always saw an empty list. **The SwiftBar panel's "Start now" did nothing** — `bash=<path>` execs its target, `ilk-run.sh` was committed 100644, exit 126, and `terminal=false` gave the failure nowhere to surface. **Speed, measured not guessed**: `--durations=25` showed **three tests were 181.25s of a 299.55s suite (61%)**, each waiting out a real 60-second bound; suite now **143s**. An earlier sleep-grep put it at 395s and was wrong — those were string literals inside stub scripts. **The batch gate now honours `baseline_red`**, which it had never read: 31 declared failures are excused and named, one undeclared still fails. |
| v0.9.72 | 2026-08-25 | **A broad gate can no longer be run in the foreground, and the instrument that measures it grew a cut point.** Opened from a measurement, not a hunch: `gate_cost.py` (new, committed with the BEFORE number at `1364cf1`) reports broad-gate cost denominated in **gate-bearing iterations only** — the denominator that matters, because iterations which never issue a broad command say nothing about gate cost and averaging over all of them is what made an earlier reading look like an improvement when none had occurred. Measured all-time over **592 iterations / 250 gate-bearing**: **308.2s per gate-bearing iteration, 53 ceiling hits, 202 in-iteration repeats**. A *ceiling hit* is a broad command that ran to the 600s harness boundary and returned **zero bytes** — with `\| tail -30` the pipeline never flushes, so the call costs 600s and yields nothing. Per project the picture is not close: **ilk-skills 106.2s/gate-iter with 0 ceiling hits and 0 repeats**, **gh-resolve 770.4s with 19 and 50**; all-time gh-resolve owns **53 of 53** ceiling hits and **193 of 202** repeats. **This tag supersedes the v0.9.71 diagnosis.** That release assumed the 600s cost was an unbounded HANG and added a per-test `--timeout=60` to `pytest.ini`. Measured, it fixes neither mechanism: a per-test timeout cannot bound total suite wall-clock, and `test_no_worktree_left_behind` — the supposed hang — passes in **2.52s** in isolation. After that change gh-resolve got *worse*, two 600s ceiling hits per iteration instead of one. The `pytest.ini` bound is **kept** (it legitimately caught one slow test, `test_restart_durability_keeps_agent_loaded`) but it is not the fix. The fix is mechanical, because guidance has now failed twice: MASTER-2026-08-24 found the correct rule already present verbatim in **both** files the worker reads (`commands/ilk.md:98`, `SKILL.md:450`) and the worker ran a bare `pytest -q` anyway. So `hooks/no-full-suite.sh` — a PreToolUse hook symlinked into `~/.claude/hooks/`, enforced rather than advisory — stops letting `ILK_ALLOW_FULL_SUITE=1` grant a **foreground** run; a foreground broad command is now denied with the concrete background+poll invocation naming `wait_for_background_output.sh`, while backgrounded runs, scoped runs, and both 2026-08-10 false-positive guards (a runner named inside a quoted string; a heredoc body of fixture text) are preserved. `plan_lint` gains `lint_foreground_whole_suite_gate`, which flags a sub-plan **authoring** a whole-suite gate at or beyond the ceiling — it fired immediately on this batch's own SP3 (`timeout: 600`), the best available evidence it works, and was left standing rather than silenced. `iteration_timing` + `collect.py` gain **within-iteration** repeat detection (the existing `find_repeated_commands` reports only commands appearing in ≥2 *distinct* iterations, so all 202 measured repeats were invisible to it) and re-aim v0.9.71's `hang_suspected` at what it actually observes — a ceiling hit with no output — without changing its detection condition. `gate_cost` gains `--after <ISO8601>`, filtering by each **iteration's** start rather than by run id, because a run can straddle the change being measured (`20260825-115525` began 11:55; the hook landed 12:00); an undatable iteration is EXCLUDED and COUNTED, never folded silently into an after-window (**8** exist corpus-wide, all 1963-6374 bytes against 700KB-2.5MB normal, so lossless here). Ceiling hits and repeats are now the **headline**; per-gate-iter is demoted to context and labelled BLENDED, since it averages projects with structurally different costs. **`ship_audit` fix:** a `#ship` trailer now satisfies the final declared step. `subplan-template.md` mandates `#ship` for the last step while the audit demanded `#step-N`, so a template-conformant sub-plan audited UNPROVEN — measured on gh-resolve, **2 of 3** UNPROVEN verdicts were spurious. A hard stop that cries wolf trains the operator to override it. **Phase 1 was deliberately NOT run as tier 3.** `gate_scope` selected tier 3, but its trigger was `gate-cost-before-2026-08-25.json` — a committed **test fixture**, i.e. the same `.json`-has-no-import-graph false positive that `.ilk-baselines/` was already exempted for. Nothing in this diff has repo-wide reach (unlike v0.9.71's global `addopts` change, which genuinely earned a whole-suite run and found a real regression). Gate actually run: **349 passed in 12.64s** over all 10 changed-module and caller targets, including `metrics.py` (collect.py's importer), `loop_status` and `run_local_checks` (ship_audit's importers). Collection floor **green: 1995 collected, 0 errors**. **Baseline floor: `could_not_compare`** — a v0.9.71 baseline exists (32 node ids) but keys on the whole-suite invocation, and feeding an empty failure list with that invocation would have manufactured a false "0 regressions across 1995 tests" from a 349-test run. So this tag carries **no node-id regression attribution**, by choice, stated rather than implied. **Not run: 6 tests** in `test_install_scheduler_autostart.py`, excluded because it drives the real production launchd label and unloaded the operator's scheduler mid-gate during v0.9.71 (filed, unfixed). **No speed improvement is claimed.** The fix landed 2026-08-25 12:00; the only post-fix data is 5 ilk-skills iterations, from the one project with 0 ceiling hits to begin with, and gh-resolve has not run since 11.5 hours before the fix. The AFTER number requires gh-resolve activity; the metric to watch is **ceiling hits 19 → 0**, near-binary and falsifiable. Also in this tag: a sub-plan shipped with all four of its per-step gate blocks removed and audited **PROVEN with `final_gate: None`** — Phase 0 cannot distinguish "gates passed" from "no gates existed". Gates restored by hand, provenance recorded in the sub-plan, filed high/high. |
| v0.9.71 | 2026-08-24 | **A bare `pytest -q` is now bounded by config, because instructing the worker twice did not work.** A live gh-resolve iteration (`20260824-161616`, iter-01) measured with `iteration_timing.py` — the reader v0.9.70's batch shipped — spent **600.5s of 615.3s tool time (97.6%) in a single call**: `python3 -m pytest -q 2>&1 | tail -30`, auto-backgrounded at the 600s harness ceiling with a **0-byte** output file and 3 pytest processes still alive. Every other one of the 72 paired calls was <=4.5s; the nine *targeted* pytest calls summed to ~13s. So v0.9.70's targeted-first guidance **worked** (10 of 11 invocations named a file) and `wait_for_background_output.sh` is now genuinely used (4 references in that iteration, replacing the hand-rolled poll loop SIGKILLed in `20260820-143915`). The residual cost was entirely the **final broad gate**. Root causes, both verified: (1) gh-resolve's `.ilk-launch.json` has three keys (`_comment`, `_iteration_timeout_rationale`, `iteration_timeout_min`) and **no `ship` block**, and `load_ship_config` returns `NotConfigured` — which **exits 0, identically to a configured project**, so no shell caller can tell them apart; measured, the loader had **2 referencing files under `skills/` (its own definition and its own test) and 0 runtime callers**. (2) The rule the worker broke was **already in both files it reads** — `commands/ilk.md:98` and `SKILL.md:450` (*"Never run a bare `pytest -q` without a per-test timeout"*). **A third restatement was therefore not the fix**, and both files are explicitly out of scope for this batch; the bound instead moved into `pytest.ini` `addopts` (`--timeout=60 --timeout-method=signal`), which the interpreter enforces and the worker cannot route around. Note `--timeout-method=**signal**`, not `thread`: the improvement-backlog candidate that seeded the batch proposed `thread`, which `SKILL.md:453` records as **known to hang** here — following the backlog would have reproduced the defect. Proof the bound holds against an unflagged invocation: `pytest skills/ilk-loop/tests/fixtures/hanging_test -q` (**no `--timeout` on the command line**) kills a 90s test at **60.02s**. Also in this tag: `ship_config.py --validate` now exits **2** unconfigured / **3** malformed / 0 configured; `doctor.py` became the loader's first runtime caller, with a regression test asserting it never goes orphaned again; and `iteration_timing.py` + `collect.py` gained a `hang_suspected` signal that distinguishes *"the suite hung and produced nothing"* from *"tests were slow"* — the live iteration is committed as `fixtures/iteration_timing/hang_600s.jsonl`, and the no-false-positive direction is asserted against the existing `broad_pytest`/`targeted_pytest` fixtures. **Phase 1 (tier 3, forced by `pytest.ini` as a path/schema change):** whole suite run twice. First run **25 failed / 1903 passed / 20 skipped / 3 xfailed / 7 errors of 1958 collected in 332.50s**, which surfaced **one attributed regression** — `test_restart_durability_keeps_agent_loaded` hit `Timeout (>60.0s)`, since its own declared waits are 30x1s + 15x2s = 60s of sleeps before any overhead. Fixed per this batch's own rule — mark the outlier `@pytest.mark.timeout(240)` with its measured basis, do **not** raise the global bound. Re-run **25 failed / 1897 passed / 7 errors in 290.39s**, **zero attributed regressions**: 20 `test_init_project` + 7 `test_meta_paths` + 1 `test_subprocess_encoding_lint` are the three declared `baseline_red` entries; 3 (`test_label_action_totality` x2, `test_watchdog_log_utf8`) are inherited `FileNotFoundError` on `powershell`, which is **not installed on this host** (neither `powershell` nor `pwsh` on PATH); 1 `test_vl_describe` was a transient DNS failure (`urlopen error [Errno 8]`) that **passed 8/8 in isolation**. **Known gaps:** the baseline floor reports **`could_not_compare` — no baseline for v0.9.70** (`.ilk-baselines/` holds v0.9.66 and v0.9.67 only), so this tag carries **no machine node-id attribution**; the attribution above was done by hand. The tier-3 cost ceiling is still missing (`TierDecision` has no cost field), unchanged since v0.9.67. And a hazard this release *discovered by triggering it*: the tier-3 gate **unloaded the operator's production launchd scheduler** — `test_install_scheduler_autostart.py` drives the real label `net.inluck.ilk.scheduler`, SIGKILLs it, and its skip guard is order-dependent, so an earlier test in the same file had already unloaded it; the daemon (PID 1807, 2d4h uptime) had to be manually restored via `launchctl bootstrap`. Filed as a high/high backlog candidate; the re-run used `--ignore` on that file, so **6 tests in it are unrun in the verifying run**. Twelve sub-plans ship in this tag's 27 commits, most of them 08-12/08-13 leftovers the loop cleared while this batch ran. |
| v0.9.70 | 2026-08-21 | **A gated sub-plan stops auditing as ungated, in three readers at once.** A sub-plan may declare gates in frontmatter, in per-step ```yaml blocks, or both; `/ilk-plan` writes `local_checks: []` in frontmatter and puts the real gates under each `### Step N`, and `_detect_local_checks.py` was already per-step aware. Three other readers were not, and each failed differently: `ship_audit.read_subplan_for_audit` returned an empty `declared_checks`, so `ship_audit.py:177` set **`final_gate: None`** no matter what the gate did; `ship_integrity.read_subplan_status_and_checks` did the same, so `evaluate_ship:69` took the *"no gate declared -- nothing to enforce"* branch and enforced nothing; and the bash driver's `test_ship_integrity` used `head -20 | grep -qE '^\s*local_checks:\s*$'`, which matches only block form in the first 20 lines, so it `continue`d past the file entirely -- missing per-step gates, `local_checks: []`, **and** any frontmatter longer than 20 lines. Measured on `MASTER-2026-08-21-loop-execution-speed`: **3 of 3** sub-plans audited `final_gate: None` while every step carried a real gate, which is why `/ilk-ship` Phase 0 had nothing to evaluate and two of the three read `PROVEN` on a gate nobody had checked. Fixed by adding **one oracle to the lowest layer all three already import** -- `run_local_checks.collect_declared_local_checks(fm_text, body)`, unioning frontmatter and per-step gates, de-duped on `(command, timeout)` -- rather than patching three parsers into three-way agreement. It reuses the existing `extract_step_local_checks`, so the audit cannot disagree with what the driver actually ran; the driver's grep is deleted and its gate-declared test folded into the Python block already in that loop, so no extra subprocess per file. **The fix is deliberately stricter:** with no recorded gate result these sub-plans now report `final_gate: 'fail'` (*"gate declared but no gate result recorded -- ship is dishonest"*) where they previously reported `None`, so Phase 0 now blocks until real outcomes are fed from the JSONL. Evidence: **3 new tests, all 3 red** against the pre-change code; caller regression across **all four** production importers of `run_local_checks` (`loop_status`, `plan_lint`, `ship_audit`, `ship_integrity`) **343 passed, 3 xfailed in 21.92s**; `bash -n` clean; `plan_lint` clean. Collection floor **green (1935 collected, 0 errors)**. **Phase 1 was NOT completed for this release.** The diff contains `run_ilk_loop_claude.sh`, so `gate_scope` selects **tier 3**, and the tier-3 whole-suite gate (**1935 tests**) was **not run** -- the same unbounded-tier-3 stall documented in v0.9.67 and v0.9.69, still unfixed (`TierDecision` has no cost field). Do not read this tag as a passing full suite; **~1592 of 1935 collected tests are unrun**. Baseline floor **`could_not_compare`** -- `.ilk-baselines/` holds v0.9.66 and v0.9.67 only, none for v0.9.69 at this invocation, so this release carries **no node-id regression attribution**. Also in this tag, from the loop rather than by hand: the three sub-plans of the loop-execution-speed batch (`iteration_timing.py`, which attributes an iteration's wall-clock by pairing `assistant`->`user` `tool_use`/`tool_result` timestamps -- available across the corpus back to 2026-05-28 with no capture change; worker guidance to prefer targeted tests, reuse the project's declared `ship.suite` invocation, and poll `wait_for_background_output.sh` instead of re-running a backgrounded command, which was **referenced 0 times** in both `SKILL.md` and `commands/ilk.md` despite shipping with a test in v0.9.66; and cross-iteration suite-result carry-forward) plus four leftover 08-13 sub-plans. The measurement that motivated the batch: over **526 of 544 iterations / 118.0 h**, model time is **69.2%** and tool execution **30.8%**, of which test commands are **23.3%** and broad-suite runs **16.6%** (**71.1%** of all test time); the loop's own repeat detector then confirmed **6** iterations of run `20260820-143915` each re-running `pytest -q` for **4804s (~80 min)**, 99.97% of that run's repeated wall-clock. **Known gaps:** `run_ilk_loop_claude.ps1:803-815` carries the **identical** defect (`Get-Content -TotalCount 20` + block-form-only match, then `return`; `$slug` also capped at 20 lines) and is **not fixed** -- the `ship_integrity.py` half reaches it since the .ps1 shells to the same script, but the pre-filter still skips these sub-plans on Windows/rezmac, and there is **no PowerShell on the authoring host**, so a patch could be neither run nor tested; `a-suite-result-outlives-its-iteration` is still **unproven** (no commit for step 3, twice, across a redo) so the loop-execution-speed batch is not clean; and the `--gate-passed` CLI default remains `unknown`, which now reads as `fail` rather than as the old silent `None`. |
| v0.9.69 | 2026-08-20 | **A red gate stops reverting every prior batch's ships.** `test_ship_integrity` in the **bash** runner walked every `*.md` in the plans dir, so a sub-plan shipped by a PRIOR batch — necessarily absent from this iteration's `local_checks` JSONL — fell through to `gate_passed='unknown'`, which `ship_integrity.py:76` calls a violation, and the runner then rewrote `status: shipped` -> `in-progress`. Measured on a consumer: **69 of 150** files reverted in one 4-minute run, and the last row of the before/after table is the signature — sub-plans shipped **and** declaring a gate went **69 -> 0**. `run_ilk_loop_claude.ps1:816-821` has carried the cross-run scoping guard all along, with a comment naming this exact failure; it was never ported. Ported now, and the enforcement path never passes `unknown` at all. New `test_ship_integrity_runner.sh` is the bash twin of the existing `.ps1` test: **8 of its 13 assertions are red without the guard.** — **A scan crash stops silently retiring a project.** `_parse_ts` returned whatever `fromisoformat` gave it, so `last_updated: 2026-08-03` parsed naive and `2026-08-03T17:30:00+08:00` aware; one master carrying both raised `TypeError` at the `min()` over `master_sub_ts`, which `scan_projects` swallowed per-project **silently** — the project vanished from every scan while the scheduler logged the generic `all-queues-empty`. Measured **2 of 9** projects raising, *including this toolkit's own*. Normalised to naive-local (both existing fallbacks are already naive, so one conversion instead of three); the `continue` stays, but the skip now lands in `SCAN_ERRORS`, on stderr, behind `--scan-errors`, and as a distinct `skip-scan-error: <keys>` scheduler reason. Two further bugs fell out: `invoke_scheduler_scan`'s exit status was masked by its `| tr` pipeline, making the scan-failure handler **dead code** (a crashed scan read as a valid empty list), and with it live `--once` looped forever without journaling. — **ilk-doctor stops giving a false all-clear.** It returned `pass: all gates clear` for a project the scheduler had been unable to see for over an hour: `master-status` said `active (runnable)`, `process-set` said `no runner processes found`, and nothing joined them. New `scheduler-visibility` gate calls `scheduler_scan` in-process (only that yields the exception the gate exists to report) — raises -> blocked, runnable-but-absent -> blocked (STRANDED), no data dir -> `unknown`, never a false pass. — **plan_lint catches the guaranteed-red gate.** Frontmatter `local_checks` run at EVERY step, so a red-first step 0 under a gate covering its red is red on iteration 1 by construction — the trigger for the revert above, where six such sub-plans all passed `plan_lint` clean. The bare pairing is deliberately **not** flagged (a gate scoped to already-green files is legitimate and this repo's own sub-plans use it); the finding needs the gate to actually cover step 0's red. Verified on real data: **0 of 64** sub-plans here, **20 of 157** on the consumer including the incident's 6. §8 now states the every-step property up front. — **A test was deleting the operator's journal.** `test_scheduler.sh`'s `log` subtest did `rm -f "${HOME}/.ilk-data/logs/scheduler.log"` against the REAL path; it destroyed ~9200 decision lines back to 2026-06-26 during this work (reconstructed from the daemon's own stdout log) and now isolates `HOME` **and** `ILK_SKILL_HOME` — both, because HOME alone makes `scheduler_scan`'s skill-root resolution raise and turns the run into a poll-forever loop. **Phase 1 was NOT completed for this release.** `gate_scope` selected **tier 3** (contract-governed `run_ilk_loop_claude.sh`) and the tier-3 gate was **not run** — a deliberate operator call against the 1870-test cost. Evidence is scoped to the four directories the diff touches: **1259 passed, 6 failed, 20 skipped, 3 xfailed** of ~1262, leaving **~608 of 1870 collected tests unrun**. Collection floor **green (1870 collected, 0 errors)**. Baseline floor **`could_not_compare`** — no v0.9.68 baseline exists for this invocation, so this release has **no node-id attribution**, and none was stored (a baseline measured from post-merge `main` would mislabel itself as v0.9.68). Of the 6 failures, **0 are attributed to this change**: 3 need an absent `powershell` binary, 2 (`test_runner_timeout_dirty_tree`) pass in isolation and are flaky under the combined run, and 1 (`test_restart_durability_keeps_agent_loaded`) is caused by the scheduler being deliberately stopped — it *skips* whenever the launchd label is loaded (`test_install_scheduler_autostart.py:143-144`), which means on a normal box it never exercises its AC-4 at all. |
| v0.9.68 | 2026-08-19 | **The release process stops poisoning its own next gate decision — and Phase 1 completes for the first time.** `store_baseline` writes `.ilk-baselines/<tag>__<hash>.json` into the repo every release; the file was git-tracked and `.json` is a `_PATH_OR_SCHEMA_EXTENSIONS` member, so `_is_path_or_schema_change` read **last release's own artifact** as a tier-3 trigger. Measured: a docs-only diff plus a stored baseline selected **tier 3**, the same diff without it **tier 0**. Fixed two ways — `.ilk-baselines/` is gitignored (independently correct: a baseline is **host-specific**, the same v0.9.66 tag measuring 746 passed/15 skipped on chad-mbp and **745/16 on rezmac**, so it was never shared state), and `gate_scope.TOOL_ARTIFACT_DIRS` exempts the tool's own artifact dirs, with `BASELINE_DIR_NAME` defined once in `gate_scope` and imported by `baseline_diff.baseline_dir` so writer and selector cannot drift apart and re-poison selection. **Demonstrated on this release:** its own diff selects **tier 1**; without the fix the `.ilk-baselines/…json` in the same diff would have forced tier 3 — **1846 tests instead of 155**. **Phase 1 completed for the first time:** tier 1, gate **155 passed**, collection floor **green (965 collected, 0 errors)**, baseline floor ran with `could_not_compare: false` / `regression_count: 0` / `filtered: false` against ref `v0.9.67` — with the caveat stated that the baseline was *stored in the same invocation*, so this release's zero is self-referential and its real value is the baseline left for v0.9.69. The newly stored `v0.9.67__c94eb5de4d3d.json` is correctly git-ignored, verifying the fix end-to-end in the real workflow. 5 new tests, 3 failing against the pre-change predicate; the exemption is deliberately narrow (a real `config/data.json`, `.ilk-launch.json`, and an `ilk-baselines-notes.json` lookalike outside the dir all still widen). 173 passed; `--source-hygiene` clean. **Docs:** new `docs/ship-gate-design.md` (the gate had no `docs/` entry while comparable subsystems do) covering why `shipped` never meant "gated", the five phases, the measured tier distribution and open limits; `skills/ilk-ship/SKILL.md` gains tier-table caveats, baseline storage rationale, the (tag, suite_invocation) keying consequence and the `select_tier`/`resolve_consumers` shape mismatch, with stale `698 collected` denominators corrected to the measured **1846 / 761**; `commands/ilk-ship.md` gains the two operator traps. **Known gaps, unchanged:** this does **not** reduce the tier-3 rate — replaying v0.9.57..v0.9.67 gives tier 3 in **8 of 10** releases both before and after, because contract-governed files drive it, and tier 0/tier 2 have never been selected on real history; **tier 3 still has no cost ceiling** (`TierDecision` has no cost field), so any release touching the driver will stall Phase 1 as v0.9.67 did; triggers remain **whole-diff not per-file**; 2 sub-plans from the 08-13 master remain unproven; and **14 `docs/` files from May-July were not reviewed** — that audit is the next task, not a claim of this tag |
| v0.9.67 | 2026-08-19 | **`shipped` stops meaning "probably gated", and the release gate refuses its own batch.** The premise `/resolver-ship` §1a rested on — *a ship commit IS a passing gate* — was false, because `test_ship_integrity()` carried **three independent shell defects**, defect 1 masking the others: `si_out=$(…) || true` then `si_exit=$?` reset the status to 0, making the violation branch **unreachable** (now `|| si_exit=$?` at `run_ilk_loop_claude.sh:1233`, with the outcome passed as scalar `--gate-passed` instead of a hand-built `--gate-json`); `grep -oP '^plan:'` is GNU-only so BSD grep exits 2 and the slug is lost (now Python); and `sed -i` without a backup suffix is a no-op revert on BSD (now Python). **The fix then caught the defect on this very batch:** `0c83e59 … shipped` precedes `ec593cd … step 3`, and `f060b6e … shipped` precedes `63b234c … step 4` — the loop shipped one step short twice and the new correction drove the missing commit afterwards. **`/ilk-ship` hard-stopped its own release.** Phase 0 audited **3 proven / 4 unproven** and refused to advance; four sub-plans had reached `shipped` with no commit for their final step (trailer search returned zero). They were re-opened, redone, and re-audited at **7 proven / 0 unproven**. The gate was not overridden. `loop_status` now prints `SHIP PROOF MISSING: N sub-plans shipped without proof` and names them — currently 2, from the 08-13 master. **Tier selection discriminates rather than always widening:** docs-only → tier 0; this diff → tier 3 **even with a healthy oracle returning zero importers**, because a path/schema change has no import graph (the oracle's zero and a real zero must never be conflated); an oracle that cannot run also defaults to tier 3, never tier 1. Evidence: Phase 0 **7/7 proven** (re-verified by hand against `git log`); collection floor **green, 1846 collected, zero errors**; scoped gates **168 passed** (`skills/ilk-ship/tests/` + `test_ship_audit.py`) and **746 passed / 15 skipped in 175.94s** (`skills/ilk-loop/tests/`). **Known gaps:** the **tier-3 whole-suite gate was deliberately NOT run** — do not read this tag as a passing full suite; the **baseline floor returned `could_not_compare`**, since baselines are keyed on (tag, suite_invocation) and the stored v0.9.66 baseline (27 known failures of 1743) is keyed to the whole-suite invocation, so **no regression attribution exists** for this release (and its `stale_exclusions` list is an artifact of an empty failure set, *not* evidence the three `baseline_red` entries are stale); **tier 3 has no cost ceiling**, so every future `/ilk-ship` touching the driver will stall exactly as this one did — first thing to fix in `gate_scope`; **2 sub-plans outside this batch are unproven** (`a-one-iteration-gate-failure-is-not-stuck`, `the-sentinel-lands-where-readers-look`) so the repo is not clean; `/ilk-ship` is **not installed** as a slash command because `install.sh --apply` aborts (exit 4) on `hooks/no-full-suite.sh`, which now ships with the toolkit but exists as byte-identical operator content, an ownership decision left to the operator; and the four re-opened sub-plans retain **stale `#ship` commits**, so counting ship-commit presence double-counts — use `ship_audit.py` |
| v0.9.66 | 2026-08-18 | A gate result now means what it says, in three places it did not. **The red-first determination is (A) enforced-but-passed, not a silent skip.** The gate path executes — `run_local_checks.py` runs the per-step block, fails (exit 1, `all_passed:false`), the B2 confirm path re-runs and confirms, and the driver sets `iter_stop_reason="local_checks_failed"` — but it fires POST-iteration (`run_ilk_loop_claude.sh:1752`, only when `total_new > 0`), by which point the agent has already committed and bumped `current_step`. It halts the loop; it cannot un-advance the step. Real case gh-resolve `b0b129b`, body "Red-first: 4 failed, 2 passed of 6 tests", `current_step` advanced to 3 with `--run-local-checks` active. A step whose purpose is to *record* failing tests cannot assert exit 0; the convention now gates on the **red count** instead, the rejected `expected_red` marker being named in §8 with its reasoning (it inverts the pass/fail label without asserting anything about the actual red state). **§8's oldest anti-pattern was prose only.** `plan_lint.py` registered 20 lints and contained **0** matches for `| tail`, `| head`, `pipefail`, or `exit status` — the rule the planner was asked to apply by hand, and on 2026-08-18 did not. Two new HARD lints take `ALL_CHECKS` **20 → 22**. Precision measured on five real shapes: `lint_exit_status_discarded` fires on `pytest -q 2>&1 | tail -10` and stays silent on `grep -q PATTERN file`, on `bash -o pipefail -c '… | tail -3'`, and on `… | tail -5 | grep -q '4 failed'` — that last is the red-first gate form this same release recommends, so the two changes are consistent rather than in conflict. `lint_broken_process_wait` catches `while ps aux | grep -q "X" | grep -v grep`, which returns instantly because `grep -q` closes the pipe so the loop tests the SECOND grep, and which licensed a concurrent second full-suite run that morning. HARD was chosen only after measuring blast radius: **0 of 333** existing sub-plans across 8 projects. **A backgrounded gate is read, not re-run.** `wait_for_background_output.sh` polls for the `[exited with code` marker rather than process absence, verified behaviourally: recorded 0 → exit 0, recorded 1 → exit 1, a present-but-**empty** file WAITS its bound instead of reporting (the exact misread — empty-because-early taken as no-result), and an unmarked file reports `inconclusive — no exit marker found in Ns (bound: Ns)` with exit 125, distinct from pass and fail. Motivating cost: gh-resolve run `20260818-154347` spent **~24 of 43 iteration minutes** re-running a suite that had already passed twice and was never red. Those figures (725.16s / 744.42s) were inflated ~35% by the retry loop running 2-3 suites concurrently; the clean measurement from run `20260818-164856` is **552s for 2857 tests**. Suite: **746 passed, 15 skipped in 175.94s** (`skills/ilk-loop/tests/`), run **by hand** on `b10e2eb` — not the loop's ship gate, because the declared wide gate produced no driver verdict. Skips are environment-conditional (PowerShell scripts absent ×2, `bash` not on PATH, worker gateway creds, `vl_hello.png` fixture, MASTER lookup). **Known gaps, none fixed here:** only **5 driver gate verdicts across the batch's 11 steps** (SP1 step 2; SP2 steps 0/1/2; SP3 step 2) — SP1 steps 0/1/3 and SP3 steps 0/1/3 produced none, including SP3 step 3, the single wide gate, so "shipped" must not be read as "every declared gate ran". The defect is **documented, not fixed**: SP2 step 0's gate actually failed (`[local_checks FAIL] … step 0 -> fail`) and shipped anyway, a live instance of the ordering described, and `run_ilk_loop_claude.sh:1752` is unchanged. 6 of 12 commits carry no `[plan:…#step-N]` trailer although `.ilk-remote-type` is `personal` (all four SP3 commits among them), and the no-trailer fallback never logged as used, so it is unproven whether it engaged. The lints are **plan-time only** — the motivating failure was an agent piping to `tail` ad hoc mid-step, which no plan-time lint catches. Deviation in the safe direction: the sub-plan said "flags", the lints emit HARD. Deliberately not shipped: `BASH_MAX_TIMEOUT_MS`/`BASH_DEFAULT_TIMEOUT_MS` stay unset — the 600s ceiling auto-backgrounds rather than kills, so it never destroyed a result, and raising it is process-global, silent, and removes the forcing function that makes slow gates visible |
| v0.9.65 | 2026-08-18 | A `tail -f` on a live iteration log stops looking wedged when it is only waiting. Measured on gh-resolve run `20260818-164856`: `iter-02` streamed **5 turns in 15.5 min against iter-01's 50 in 30.3 min**, and the two differed in a signal the log never showed — **0 SSE `ping` keepalives in iter-01 vs 80 in iter-02**, i.e. 800s of the 930s iteration (86%) spent at time-to-first-token with the connection open and idle. A 117s arrival trace put the duty cycle at **5.3s of streaming in 117s (4.5%)**; inside a burst the deltas still arrive 0.02-0.12s apart, so throughput was never the problem. The local pipeline was ruled out *positively* rather than by inspection: on the sample ticks where data arrived, `iter-02.log` and `iter-02.log.jsonl` carried the same mtime, and every `print()` in the renderer already passed `flush=True`. What the log did instead was hide all of it — `thinking_delta` is dropped (`_stream_json_render.py:94`) while its paired `system/thinking_tokens` event still printed one content-free line each, **18190 of iter-01's 18440 lines (98.6%)** being the bare string `[system] thinking_tokens`, and `ping` fell through the trailing `else: return` so a 160s stall emitted nothing at all. The renderer now stamps every standalone line `[HH:MM:SS]`, coalesces a thinking block into one `N chars in Xs` summary (progress line every `ILK_RENDER_THINK_SEC`, default 5), and turns the pings into a cumulative `[waiting Ns] with no output from the API` heartbeat (`ILK_RENDER_WAIT_SEC`, default 30 — a judgment call against the measured 10.0s ping cadence and 35-160s gaps, so a typical stall yields 1-5 lines rather than one per ping; wrong if another endpoint pings slower, hence the env override). Replaying both real JSONL files: iter-01 **18,440 → 301 lines / 471 KB → 20.6 KB**, iter-02 1,502 → 368; marker counts identical on both (`[tool >]` 53/53 and 69/69, `[result <]` 48/48 and 60/60, `[result !]` 5/5 and 8/8, `[done]`, `[init]`) and assistant prose byte-for-byte unchanged after stripping stamps. `[system] api_retry` gains its detail (`attempt=1/10 delay=619ms error=unknown` — the one such event in iter-02 is the endpoint-health signal), and `collect.py:filter_system_lines` now **keeps** it instead of discarding it as `[system]` noise, while tolerating the new stamp prefix — it matched on `startswith("[system] ")` and would otherwise have silently stopped matching at all. The renderer had **0 test files** (grep over `skills/**/*.py|*.sh`: referenced only by itself and `run_ilk_loop_claude.sh:1251`) and is now a state machine, so it gets 7; 5 fail against the pre-change file, the other 2 being deliberate marker-parity guards. 214/214 `ilk-feedback` tests pass; both changed files compile on the runner's Python 3.9.6. Unchanged on purpose: the slowness itself — the mimo endpoint is a deliberate token-economics choice — and the PowerShell mirror (`run_ilk_loop_claude.ps1:1130`), whose divergence the renderer docstring now states rather than claiming parity |
| v0.9.64 | 2026-08-16 | The menu-bar panel stops lying about a running loop, from two independent causes. **A `queued` master is the current master.** `status_all` decided which master is current with a literal `status == "active"` — a second, narrower definition than the `loop_status.pick_active_master` that `/ilk-status` uses. Nothing promotes a master mid-run: `queued → active` is written only by `promote_next_master.py`, which the watchdog calls *after* a run exits (`watchdog.sh:851`), while the live loop merely **peeks** the top queued master (`loop_status.py:176-179`, explicitly *"do NOT promote"*). On gh-resolve the run started 12:58:43 and `MASTER-2026-08-16` was authored at 13:10 — 11 minutes in; the loop rolled straight from 15e into it via peek, never exited, so nothing ever promoted it. Zero of the project's masters were `active`, and the panel rendered `* users-…-gh-resolve running on mimo-v2.5-pro` with no master, no sub-plan and no step while `/ilk-status` showed `a-draft-is-checked-before-it-is-filed step 3/5` for the same instant. This is the master-level twin of v0.9.62's sub-plan drift, and the same remedy: call the canonical resolver instead of re-deciding. Display now follows the loop (active **or** queued) while the scheduler-facing flags keep their strictly-`active` meaning via `master_is_active` — a queued master is not auto-dispatchable, promotion is what makes it so. The first attempt conflated the two and turned every queued master `runnable`; the AC-6 guards in `test_status_all_actions.py` caught it. `pick_active_master`'s rules 4-5 fall back to newest-by-mtime among paused/shipped/draft masters so its own table renders a row — that result is rejected here, since a terminal master has no runnable sub-plan and `_blocked_info`'s `stalled` rule would then flag every finished project as needing a human. **The third cause left open in v0.9.62 is closed.** `TestLiveRunnerTimeout` drove the real runner with `dict(os.environ)`, so `ilk_paths.py:292` fell through to `~/.ilk-data` and every run registered its pytest tmpdir as a production project; the mock claude is killed by `gtimeout`, so some sentinels were left at `state=running` with a dead PID — `blocked:stale-running` forever, and the idle filter cannot drop them because `blocked` outranks `idle`. 32 of 32 leaked dirs carrying a launcher JSON traced to these two tests (17 + 15), out of 109 under `~/.ilk-data/projects`. Fixed by redirecting `ILK_DATA_HOME`; verified positively — the two tests pass and the dir count no longer moves, where each prior run added 2. `status_all` additionally gains an **`orphaned`** field (repo_path resolved but gone from disk) which both renderers skip before computing an icon; it is a field rather than a drop because nine such dirs hold real plan history from pre-move paths, one with 21 masters. Named `orphaned`, not `stale`, since `stale` already means a sentinel claiming running with a dead PID. Panel on the affected Mac: 3 rows (2 permanent pytest `!` alerts) → 1. Also: rows read `<sub-plan> <step>` like `/ilk-status` instead of the inverted `3/5 a-draft-is-checked-…`, and a missing `from __future__ import annotations` in `test_loop_status_json_clean.py` no longer aborts collection for the entire `skills/ilk-loop/tests/` directory on the system Python 3.9 (722 collected, 707 passed / 15 skipped). Unchanged: the 3 `skills/ilk-watchdog/` failures needing PowerShell, red on a clean tree on this host |
| v0.9.63 | 2026-08-14 | The planner-gates batch: nine sub-plans closing the gap between "the planner blessed it" and "the loop can run it". **`plan_lint` gains git awareness** — a `scope_paths` entry absent from the base branch but present on another ref is now a HARD finding, discriminated three ways (on base → OK; nowhere in history → OK, it is a new file; absent-on-base-but-present-elsewhere → finding), which is what makes it false-positive-free. A master now declares `base_branch:` and a HARD master-level gate rejects a batch whose sub-plans straddle two branches — the single-repo analogue of meta-mode's `repo:` gate, generalised from the same stated rationale ("would route commits to the wrong member repo and corrupt the ship report"). `/ilk-plan` gains **step 4e** emitting a `Branch targets` table beside the Fixtures and Env-prereqs tables, because the existing scans get run *because* each produces a visible artifact. A new lint enforces §8's per-file-gate-on-a-shared-module rule, which was prose-only and had let a return-type change ship with an unupdated production caller (14 iterations, 190 min, 34 commits on a consumer project); it fires on 6 of 376 corpus sub-plans (1%), well inside its 15% precision budget. `plan_lint` also now validates gates with **the runtime's own parser** rather than a YAML conformance check — the originating report blamed invalid YAML, but `run_local_checks.py:77` is a hand-rolled reader, both cited "invalid" cases parse correctly, and `pyyaml` is not importable on this host, so the proposed hard lint would not have run and would have false-positived where it did. On the signal side: a one-iteration gate failure is no longer labelled `local-checks-stuck` (the L1 sentinel map at `collect.py:1262` had no iteration guard while the windowed path at `:1001` already required ≥3), a failing gate now records **which command failed and a bounded output tail** (the data already existed in `CheckResult`; the runner's hand-interpolated JSON discarded it), the postmortem tail skips `[system]` noise, and `improvement_backlog list` no longer dies with `TypeError` on records predating five required fields — which had made `/ilk-self-improve` non-functional end to end. The exit sentinel moves to `runtime/launcher/`, where its eight readers were already looking; **that move broke 12 test fixtures still writing the old path**, caught only by a baseline-compared whole-suite run. Known gaps stated plainly: 3 of 9 sub-plans reached `shipped` without their final step's commit (see `ilk-loop-ships-one-step-short-handoff.md`), and 29 suite failures remain that were **already red at v0.9.62** — 20 in `ilk-lark-tickets/test_init_project.py`, 7 collection errors in `ilk-loop/scripts/test_meta_paths.py` |
| v0.9.62 | 2026-08-14 | The tray stops naming a **blocked** sub-plan as the one being worked. `status_all._resolve_next_subplan` skipped only `shipped`, so the first non-shipped sub-plan won — and `blocked` is non-shipped. Observed on gh-resolve: the SwiftBar panel read `2/4 shadow-mode-provisioning running on mimo-v2.5-pro` while pid 57457 (run `20260814-013546`) was in fact working `first-party-means-our-repo` at 1/5; `shadow-mode-provisioning` had been `blocked` at 2/4 for hours. `loop_status.py` had it right, so the two disagreed about the same question — and `plan_status.master_has_runnable` already carried the rule in its docstring (`plan_status.py:177-181`): *"a blocked sub-plan is outstanding work, but it is NOT runnable — nothing the loop does will advance it until a human unblocks it."* Both call sites were wrong for the same reason: the active-master branch feeds the tray, and the queued-master branch feeds `manually_runnable`, so a blocked-only queued master advertised work the loop could never pick up. Fixed by **importing** `plan_status._RUNNABLE_SUBPLAN_STATUSES` rather than re-declaring the set — a second literal is precisely how these two drifted — with a test asserting the import is the source, by value plus a source check rather than object identity (identity fails spuriously in a full-suite run, where the two modules load via different `sys.path` entries). Note this is a *different* cause from v0.9.61's phantom: there the PID had been recycled onto an unrelated process; here the PID is a genuine live runner and the sub-plan label alone was wrong. A third cause remains open (`8b73476e` / `d1426c98`): tests that spawn a real runner against a pytest tmp project register it in the production data root, where it is correctly counted as a live loop because it *is* one — 55 of 75 project dirs are now such junk |
| v0.9.61 | 2026-08-13 | The menu bar stops counting loops that ended weeks ago. v0.9.55 taught the *bash* side to verify the process behind a pidfile (`_ilk_pid.sh:ilk_pid_alive`) rather than ask `kill -0`, but the Python status readers were missed — and `_ilk_pid.sh`'s own comment asserted otherwise ("Mirrors pid_health.pid_command_alive … already used by status_progress/status_all"), while `pid_command_alive` was in fact called from **0 of 4** sentinel-liveness sites. State-gating cannot close this gap on its own: it only covers runs that reached `Finalize-Sentinel`, and a run killed before that keeps `state="running"` forever, leaving the PID as the only remaining evidence. A gh-triage sentinel written 2026-07-08 named PID 18920, which on 2026-08-13 at 11:14:49 the OS handed to an unrelated `zsh … pytest` shell: the SwiftBar title read `ilk 2*` against one live loop, and because the phantom's master was empty its row showed no step and no sub-plan — so the panel appeared to contradict its own icon. `pid_health` gains `pid_cmdline` (full argv via `ps -ww` / `/proc/<pid>/cmdline` / CIM — the *base* name is `bash` for every runner and so cannot tell one from any other shell, which is why the existing `pid_command_alive` was the wrong instrument) and `ilk_pid_alive`, now used by `status_all` (tray/xbar/dashboard), `ilk_watch`, the launcher's `status_all`, and `status_progress`. An unreadable command line still falls back to bare liveness — over-reporting a run as live is the safe direction, since the stale-running path is what parks a project. A stale sentinel now surfaces as `!` stale-running instead of as a running loop. Five status tests asserted liveness on `os.getpid()` and went red on the new semantics, correctly: the pytest process is exactly the sort of unrelated command a recycled PID lands on. They and six neighbours take a new `live_ilk_pid` fixture that spawns a genuinely runner-shaped process, and a test pins the pattern list to `_ilk_pid.sh`'s, since a divergence between the two is a status display that contradicts the scheduler |
| v0.9.60 | 2026-08-12 | `plan_lint` stops treating a directory argument as a scope. `_is_whole_suite_command` counted *any* positional arg as scoping, so `pytest tests/ -q` escaped `lint_wholesuite_gate_baseline` entirely while a bare `pytest -q` was caught — it judged the gate form almost nobody writes and skipped the two most common gates in the corpus (`python3 -m pytest tests/ -q`, 33 occurrences; `pytest tests/ -x -q`, 17). A directory scopes nothing the lint cares about: pytest collects the whole tree, so one collection error under it fails the gate — and 28 of the 47 hidden sub-plans declare that gate in **frontmatter**, which re-runs at *every* step, so a red tree false-blocks every step and can drive the project to `stuck-no-progress`. The old rule was also accidentally inconsistent — `"tests"` sits in `_NON_PATH_TOKENS`, so `pytest tests -q` was whole-suite while `pytest tests/ -q` was not; a trailing slash decided it. Now a directory tree is whole-suite, a single file and a `::node_id` stay scoped, `-k`/`--deselect` still scopes, and value-taking flags no longer leak their argument into the path list. Over 333 real sub-plans: effectively-whole-suite gates judged **84 → 123**, findings **208 → 306**. Still open (`b37ce4609f15cd21`): the `baseline-green` escape is accepted on the presence of the phrase and never verified, which silences 26 of those 123 |
| v0.9.59 | 2026-08-12 | Diagnosability: one command now answers *"why is nothing running for this project?"*. New read-only `/ilk-doctor` walks 8 gates in order and stops at the first blocker, printing the artifact each gate consulted — and its primary signal is progress across **two** samples, because a 15-minute foreground gate is byte-identical to a stall in any single one. `plan_lint`'s gate lints can finally see per-step `local_checks`: 12 of 13 call sites read frontmatter only, so ~11 gate lints inspected roughly a fifth of the gates they claimed to cover (the same `pytest -q` gate scored 1 finding in frontmatter and **0** in a per-step block — which is why a gh-resolve step gated on a baseline-red 2152-test suite passed lint clean, then failed three times across ~90 min). Re-linting 333 sub-plans surfaces 123 newly-visible findings, 85% of them whole-suite gates with no verified baseline. A new `lint_redundant_gate` stops the defect being authored at all: a step body must not instruct a command its own `local_checks` already runs, since the driver runs gates *after* the commit. `stop.sh` no longer kills itself, a bystander shell whose argv merely contains the project path, or the launchd-supervised scheduler — killing that one made launchd restart it and re-dispatch the still-runnable master, so "stop" caused a relaunch; it now verifies the tree is gone before reporting success. Postmortems regain their tail under the `runs/` layout, and a run that left no JSONL records says so instead of raising `KeyError` |
| v0.9.58 | 2026-08-12 | A project's declared `iteration_timeout_min` is finally honoured: `read_project_config` never looked at `<project_root>/.ilk-launch.json` — the location `/ilk-plan` step 8a already documents — so gh-resolve declared 60, silently got the 30-minute default, and had its last step killed mid-suite twice |
| v0.9.57 | 2026-08-12 | One runner per project, enforced by the kernel: the runner now acquires a per-project `flock` and re-execs under it, so a second runner is refused (exit 3) before it writes a sentinel, a run dir or `running.pid` — closing the fan-out that put 10 runner processes and 4 concurrent pytest interpreters in one working tree. Dispatch-side busy checks additionally ask the process table rather than trusting a `running.pid` that tracked 1 of 10 live runners. Portable via `fcntl.flock` + cleared `FD_CLOEXEC` (`flock(1)` does not exist on macOS), and the kernel releases it even on `SIGKILL` |
| v0.9.56 | 2026-08-12 | The scheduler stops re-dispatching a drained-but-blocked master (96 run dirs in 8h, 90 empty — a `blocked` sub-plan is outstanding work but not *runnable*), and the runner stops reporting such a batch as "all shipped"; an iteration's own command file no longer tells the worker to background long gates that can never resume across the iteration boundary; the auto-quarantine path no longer dies on an unbound `$SKILL_ROOT`, a crash that also erased the JSONL record the postmortem needs; `/ilk-plan --yes` skips the grouping wait for one invocation — a flag, not a timer |
| v0.9.55 | 2026-08-10 | A recycled PID no longer wedges the queue: the sentinel, the scheduler's own lock and the launcher all verify the process behind a pidfile instead of asking `kill -0`. A 20-day-stale `running.pid` whose PID had been reused by an unrelated shell held one project at `skip-busy` on every poll. `ilk_pid_alive` is now one shared helper rather than a per-script copy |
| v0.9.54 | 2026-08-10 | A watchdog can no longer outlive the loop it supervises: `loop_status` was invoked with an unsupported `--project` flag so `advance` was unreachable and every drained queue span forever; both keep-alive paths are now liveness-aware and bounded |
| v0.9.53 | 2026-08-10 | Loop fidelity: worker slot homes get `commands/` so slots above 0 can run at all; a never-ran or throttled run no longer parks the project as `stuck-no-progress`; a timed-out iteration preserves its dirty tree and reports what consumed the budget; the dashboard resolves the real repo so pace/ETA work; `plan_lint` flags gates whose selector can silently select nothing |
| v0.9.52 | 2026-08-04 | Public-release readiness: internal IDs scrubbed, stale tests fixed, first CI, README as an OSS landing page |
| v0.9.51 | 2026-08-03 | `local_checks` gate actually runs on a shared remote (trailer-independent target discovery, verified end-to-end); one `run_id` per run; MASTER registry rows reconciled |
| v0.9.50 | 2026-08-03 | Scheduler skips a resolved-but-absent repo path instead of re-dispatching it forever; test fixture + runner hang fixes |
| v0.9.49 | 2026-07-30 | Dispatch planner verification when a master drains (closes the last manual join); AC-6 escalation guard |
| v0.9.48 | 2026-07-28 | Same-day plan slugs (`YYYY-MM-DD<letter>-`) parse across all eight call sites via shared `plan_slug.py` |
| v0.9.47 | 2026-07-26 | `supervised_only` scope guard: decoupled from autonomy tiers, narrow trigger enforced in `plan_lint` |
| v0.9.46 | 2026-07-04 | Native-IO hygiene + watchdog stale-non-success race fix |
| v0.9.45 | 2026-07-02 | macOS detached-runner verified PASS-with-caveat |
| v0.9.44 | 2026-07-01 | Real detached-runner steer-hook smoke test |
| v0.9.42 | 2026-07-01 | Hermetic `ilk-loop` test suite (path-length / pytest-timeout / powershell guards) |
| v0.9.41 | 2026-07-01 | Steer-hook bash parity (`steer_hook.sh` + `.sh` runner wiring) |
| v0.9.40 | 2026-07-01 | Operator steer-hook + vision-as-a-tool + vision-stall classification |

## Earlier milestones

The notes below were previously kept in `README.md`. They describe the shape of
the toolkit as it grew, and are preserved here rather than at the top of the
README.

### v0.8.13 — empty-repo resilience, `/ilk-schedule`, macOS monitoring

- **Empty-repo / stale-state resilience.** A run pointed at a freshly
  `git init`'d repo (branch but zero commits) no longer cascades into a hard
  stop: the runner degrades a missing `HEAD` to `(unknown)` instead of leaking a
  fatal, `collect.py` classifies a run that died before iter 1 as `interrupted`
  (instead of failing to produce a postmortem), the watchdog ignores a
  stale/contradicted sentinel and cross-checks `loop_status` before declaring
  "queue drained", `promote_next_master` treats `pending` as a `queued` alias,
  and the launcher's active-guard ignores a finished-but-lingering loop window.
- **`/ilk-schedule`** *(new slash command)* — launches the single cross-project
  scheduler detached, the way `/ilk-run` launches the per-project loop+watchdog.
  Use it to drain **all** projects' queues from one supervisor instead of one
  watchdog per project. `scheduler.{ps1,sh}` gained `-Detach` / `--detach`.
- **Monitoring surfaces** (esp. for macOS, where detached runs are headless
  `screen` sessions with no visible window):
  - `loop_status.py --json` + `status_all.py` — machine-readable per-project and
    all-projects status.
  - `/ilk-status --watch` (bash `--watch` / PowerShell `-Watch`) — a
    self-refreshing all-projects + slots cockpit.
  - **Desktop notifications** on watchdog/scheduler events (ship, blocked,
    restart, postmortem-failed, queue-drained). macOS `osascript`, Windows
    toast, Linux `notify-send`. Gated by `ILK_NOTIFY` (`0` disables).
  - **tmux slot sessions** — set `ILK_MULTIPLEXER=tmux` so the scheduler runs
    each slot in a named `ilk` session; `screen` stays the fallback.
  - **xbar / SwiftBar menu-bar plugin** (`tools/xbar/`) — glanceable macOS
    menu-bar status.

### v0.8.10 — planner/worker cost split + cross-project scheduling

- **`claude-worker` launcher engine.** Route the detached loop under the cheap
  worker home (`~/.claude-worker`) instead of the planner's official provider,
  for unattended / cost-sensitive runs (`--engine claude-worker`, or
  `{ "worker_engine": "claude-worker" }` in `.ilk-launch.json`). See
  [`skills/ilk-launcher/references/worker-engine.md`](skills/ilk-launcher/references/worker-engine.md).
- **Machine-wide default opt-in.** Set `ILK_DEFAULT_ENGINE=claude-worker` to
  default a whole machine to the worker without editing every project.
  Precedence: CLI `--engine` > `.ilk-launch.json` > `ILK_DEFAULT_ENGINE` >
  `claude`. The shipped default stays `claude`; a real `claude-worker` launch
  fails closed if the worker home isn't bootstrapped.
- **Cross-project scheduler (V1).** A single daemon
  (`skills/ilk-watchdog/scripts/scheduler.{ps1,sh}`) drains every project's
  queue FIFO, one at a time, routed through the worker engine — per-project
  sentinel mutex, poll/idle/auto-wake, pool cap 1, global budget ceiling,
  non-starving blacklist skip.
- Earlier: a one-command `claude-worker` PATH installer (v0.8.7) and the dual
  planner/worker Claude homes design.

### v0.5 — Codex parity

All skills and slash commands install under `~/.codex/skills/` and
`~/.codex/commands/` alongside Cursor and Claude Code; hardcoded `~/.cursor`
paths replaced with a skill-root resolver, command prompts made host-neutral,
and the launcher gained the `worker_engine` config + `--engine` override. New
`ilk-runner` skill plus `/ilk-run` and `/ilk-status` slash commands sequence
launcher + watchdog for supervised unattended runs.
